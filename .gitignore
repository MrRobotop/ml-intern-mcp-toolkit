# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# Env (keep .env.example as the committed template)
.env
.env.*
!.env.example

# Test / coverage
.coverage
coverage.xml
htmlcov/
.pytest_cache/

# Tooling caches
.ruff_cache/
.mypy_cache/

# Project caches
tests/fixtures/cache/
~/.cache/arxiv-deep/

# Demo artefacts produced at runtime
demo/last_run.transcript.txt
demo/output/

# Binary fixtures
*.pdf
*.png
!docs/images/*.png

# Build
dist/
build/

# Claude Code per-user settings (project-shared settings live in .claude/settings.json)
.claude/settings.local.json
# Claude Code worktree scratch space (created by isolation: worktree subagents)
.claude/worktrees/

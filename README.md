# ml-intern-mcp-toolkit

MCP toolkit extending Hugging Face's `ml-intern` with deep arxiv reading (`arxiv-deep`) and experiment tracking (`experiment-tracker`).

This README is a placeholder. The full public-facing README lands in Phase 5.
